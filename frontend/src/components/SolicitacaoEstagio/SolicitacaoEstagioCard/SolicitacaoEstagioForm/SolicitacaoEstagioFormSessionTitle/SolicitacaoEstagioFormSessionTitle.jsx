import React from 'react';
import Wrapper from './SolicitacaoEstagioFormSessionTitle.styled';

export default function SolicitacaoEstagioFormSessionTitle({ children }) {
  return <Wrapper>{children}</Wrapper>;
}
