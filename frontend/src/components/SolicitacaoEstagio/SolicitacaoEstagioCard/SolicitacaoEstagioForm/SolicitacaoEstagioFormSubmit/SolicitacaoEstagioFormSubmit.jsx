import React from 'react';
import Wrapper from './SolicitacaoEstagioFormSubmit.styled';

export default function SolicitacaoEstagioFormSubmit({ value }) {
  return <Wrapper value={value} type="submit"></Wrapper>;
}
